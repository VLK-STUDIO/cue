---
title: Getting Started
description: Install overlay-manager and open your first dialog.
---

`@overlay-manager/react` lets you register a dialog once, then open it from a click handler, a route effect, or another overlay.

## Installation

```npm
npm i @overlay-manager/react
```

## Add the provider

Render `OverlayProvider` once near the root of your app.

```tsx
import { OverlayProvider } from "@overlay-manager/react";

export function App() {
  return (
    <OverlayProvider>
      <Page />
    </OverlayProvider>
  );
}
```

## Create an overlay

Define the overlay next to the component it renders. Spread `OverlayProps` (`open`, `onOpenChange`) onto your dialog so Escape and backdrop clicks still close it.

```tsx
import { createOverlay, type OverlayProps } from "@overlay-manager/react";
import { Dialog } from "./dialog";

export const settingsDialog = createOverlay((props: OverlayProps) => (
  <Dialog {...props} title="Settings">
    <button type="button" onClick={() => settingsDialog.close()}>
      Close
    </button>
  </Dialog>
));
```

## Open it

Call `open()` from anywhere. No local `useState`.

```tsx
settingsDialog.open();
```

<WelcomeDemo />

Works with any component that accepts `open` and `onOpenChange`: Radix Dialog, Base UI, vaul, or your own.
