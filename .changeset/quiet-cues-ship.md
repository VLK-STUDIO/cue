---
"@vlkoss/cue": major
---

Make `createCue()` the only way to create overlays. This release adds isolated Cue environments, typed shared components, and provider-level backdrops while removing the global overlay API and `unmount: false`.
