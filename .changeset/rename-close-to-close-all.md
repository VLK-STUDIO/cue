---
"@vlkoss/cue": minor
---

Rename handle `close` to `closeAll` so definition-level close-all is obvious next to instance `close`. Add JSDoc on `createOverlay` and its methods. Mark `OverlayProvider` with `"use client"` and document Next.js App Router usage.
