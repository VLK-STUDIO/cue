---
"@vlkoss/cue": minor
---

Always unmount closed instances after `delay` (drop `unmount: false`). Allow bare `open()` / `openAsync()` when every props key is optional. Stop exporting `OverlayManager` from the public package entry.
